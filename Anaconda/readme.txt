Install with:

conda env create -f conda_env.yml


Afterwards, install Ipython kernel (for Jupyter Notebook) with:

conda activate 22_dompc_paper
python -m ipykernel install --user --name 22_dompc_paper